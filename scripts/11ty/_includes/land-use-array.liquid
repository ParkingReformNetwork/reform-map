{%- if array.size == 1 -%}
  {% render "land-use-body" record: array[0] %}
{%- else -%}
  {%- for item in array -%}
    <section>
      <h3>Policy record {{forloop.index}}</h3>
      {% render "land-use-body" record: item %}
    </section>
  {%- endfor -%}
{%- endif -%}
