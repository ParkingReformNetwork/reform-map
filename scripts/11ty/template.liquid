---
pagination:
  data: entries
  size: 1
  alias: entry
permalink: "{{ entry.escapedPlaceId }}.html"
layout: layout.liquid
---
<main>
  <header>
    {% if entry.place.supplemental -%}
      <h1>Parking reforms in {{ entry.place.name }}<br/><span class="supplemental-place-info">{{ entry.place.supplemental }}</span></h1>
    {%- else %}
      <h1>Parking reforms in {{ entry.place.name }}</h1>
    {%- endif %}
  </header>
  <ul class="place-details">
    <li>{{ entry.population }} residents</li>
    {% if entry.repeal -%}
      <li>All parking minimums removed</li>
    {%- endif %}
  </ul>
  {% if entry.benefitDistrict.size > 0 -%}
    <section>
      <h2>Parking benefit district{% if entry.rmMin.size > 1 %}s{% endif %}</h2>
      {% render "benefit-district-array" array: entry.benefitDistrict %}
    </section>
  {%- endif %}
  {% if entry.rmMin.size > 0 -%}
    <section>
      <h2>Parking minimum removal{% if entry.rmMin.size > 1 %}s{% endif %}</h2>
      {% render "land-use-array" array: entry.rmMin %}
    </section>
  {%- endif %}
  {% if entry.reduceMin.size > 0 -%}
    <section>
      <h2>Parking minimum reduction{% if entry.addMax.size > 1 %}s{% endif %}</h2>
      {% render "land-use-array" array: entry.reduceMin %}
    </section>
  {%- endif %}
  {% if entry.addMax.size > 0 -%}
    <section>
      <h2>Parking maximum{% if entry.addMax.size > 1 %}s{% endif %}</h2>
      {% render "land-use-array" array: entry.addMax %}
    </section>
  {%- endif %}
</main>
